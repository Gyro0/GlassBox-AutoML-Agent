"""GlassBox AutoML — a transparent, NumPy-only AutoML library.

The package is organised into focused submodules:

* ``glassbox.eda``           — automated exploratory data analysis
* ``glassbox.preprocessing`` — imputers, scalers, and encoders
* ``glassbox.models``        — scratch-built estimators
* ``glassbox.optimization``  — grid/random search and K-fold CV
* ``glassbox.evaluation``    — classification and regression metrics
* ``glassbox.agent``         — AutoFit pipeline and IronClaw/MCP entrypoints
"""

__version__ = "0.1.0"
